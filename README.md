# Secure-Azure-App-Starter
The project is meant to solve the problem of having "loose code" in azure environments. It achieves this by scanning the entire codebase for loose ends and refuses commits when it discovers any in the codebase. This project looks small but could be the reason why a company does not suffer an attack because it has already covered up most of the CVEs.
